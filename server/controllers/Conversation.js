const router = require('express').Router();
const {Conversation} = require('../models');

// creating new conversation with another user
router.put('/Conversations');
//getting a conversation already created with another user
router.get('/Conversations');
//getting conversation between two users
router.get('/Conversations');